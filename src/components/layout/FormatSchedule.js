import React from "react";
import MatchupCard from "../games/MatchupCard";
import GameGrid from "./GameGrid";

export default function FormatSchedule({ schedule, selectTeam, team }) {
  const formattedSchedule = 
  return <div></div>;
  // const formattedSchedule =

  // if (daySchedule) {
  //   const formattedSchedule = daySchedule.map((game, i) => {
  //     return (
  //       <MatchupCard
  //         selectTeam={selectTeam}
  //         gameInfo={game}
  //         team={team}
  //         key={i}
  //       />
  //     );
  //   });
  //   return <GameGrid schedule={formattedSchedule} />;
  // }
}
